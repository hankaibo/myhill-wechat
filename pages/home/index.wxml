<!--pages/home/index.wxml-->
<view class="page" data-weui-theme="{{theme}}">
	<mp-tabs tabs="{{tabs}}" swiperClass="weui-tabs-swiper" activeClass="tab-bar-title__selected"
		tabActiveTextColor="#07c160" activeTab="{{activeTab}}" tabBackgroundColor="{{theme==='dark'?'#191919':'#ffffff'}}"
		tabInactiveTextColor="{{theme==='dark'?'hsla(0,0%,100%,.5)':'#000000'}}" bindtabclick="onTabCLick"
		bindchange="onChange">

		<!-- 全部 -->
		<scroll-view class="tab-content" slot="tab-content-0" scroll-y="true" style="height: calc(100vh - 51px)"
			bindscrolltoupper="upper" bindscrolltolower="lower">
			<view class="tab-content__bd">
				<mp-cells>
					<mp-cell wx:for="{{allList}}" wx:key="id" hover data-field="{{item.id}}" bindtap="handleView">
						<image wx:if="{{item.imgUrl}}" slot="icon" src="{{item.imgUrl}}" alt="封面"
							style="margin-right: 16px;vertical-align: middle;width:120px; height: 120px;" />
						<view wx:else slot="icon" style="margin-right: 16px;width:120px; height: 120px; background: #ccc" />
						<view class="weui-media-box__bd">
							<h4 class="weui-media-box__title">{{item.name}}</h4>
							<view class="weui-media-box__desc">
								<view><text>{{item.startTime}}</text></view>
								<view><text>{{item.place}}</text></view>
								<view><text>{{item.description}}</text></view>
								<view class="weui-media-box__add">
									<mp-icon type="outline" icon="add" color="block" size="{{9}}"></mp-icon>加入
								</view>
							</view>
						</view>
					</mp-cell>
				</mp-cells>
			</view>
		</scroll-view>
		<!-- 学习 -->
		<scroll-view class="tab-content" slot="tab-content-1" scroll-y="true" style="height: calc(100vh - 51px)"
			bindscrolltoupper="upper" bindscrolltolower="lower">
			<view class="tab-content__bd">
				<mp-cells>
					<mp-cell wx:for="{{studyList}}" wx:key="id" hover data-field="{{item.id}}" bindtap="handleView">
						<image wx:if="{{item.imgUrl}}" slot="icon" src="{{item.imgUrl}}" alt="封面"
							style="margin-right: 16px;vertical-align: middle;width:120px; height: 120px;" />
						<view wx:else slot="icon" style="margin-right: 16px;width:120px; height: 120px; background: #ccc" />
						<view class="weui-media-box__bd">
							<h4 class="weui-media-box__title">{{item.name}}</h4>
							<view class="weui-media-box__desc">
								<view><text>{{item.startTime}}</text></view>
								<view><text>{{item.place}}</text></view>
								<view><text>{{item.description}}</text></view>
								<view class="weui-media-box__add">
									<mp-icon type="outline" icon="add" color="block" size="{{9}}"></mp-icon>加入
								</view>
							</view>
						</view>
					</mp-cell>
				</mp-cells>
			</view>
		</scroll-view>
		<!-- 活动 -->
		<scroll-view class="tab-content" slot="tab-content-2" scroll-y="true" style="height: calc(100vh - 51px)"
			bindscrolltoupper="upper" bindscrolltolower="lower">
			<view class="tab-content__bd">
				<mp-cells>
					<mp-cell wx:for="{{playList}}" wx:key="id" hover data-field="{{item.id}}" bindtap="handleView">
						<image wx:if="{{item.imgUrl}}" slot="icon" src="{{item.imgUrl}}" alt="封面"
							style="margin-right: 16px;vertical-align: middle;width:120px; height: 120px;" />
						<view wx:else slot="icon" style="margin-right: 16px;width:120px; height: 120px; background: #ccc" />
						<view class="weui-media-box__bd">
							<h4 class="weui-media-box__title">{{item.name}}</h4>
							<view class="weui-media-box__desc">
								<view><text>{{item.startTime}}</text></view>
								<view><text>{{item.place}}</text></view>
								<view><text>{{item.description}}</text></view>
								<view class="weui-media-box__add">
									<mp-icon type="outline" icon="add" color="block" size="{{9}}"></mp-icon>加入
								</view>
							</view>
						</view>
					</mp-cell>
				</mp-cells>
			</view>
		</scroll-view>
		<!-- 绘画 -->
		<scroll-view class="tab-content" slot="tab-content-3" scroll-y="true" style="height: calc(100vh - 51px)"
			bindscrolltoupper="upper" bindscrolltolower="lower">
			<view class="tab-content__bd">
				<mp-cells>
					<mp-cell wx:for="{{painingList}}" wx:key="id" hover data-field="{{item.id}}" bindtap="handleView">
						<image wx:if="{{item.imgUrl}}" slot="icon" src="{{item.imgUrl}}" alt="封面"
							style="margin-right: 16px;vertical-align: middle;width:120px; height: 120px;" />
						<view wx:else slot="icon" style="margin-right: 16px;width:120px; height: 120px; background: #ccc" />
						<view class="weui-media-box__bd">
							<h4 class="weui-media-box__title">{{item.name}}</h4>
							<view class="weui-media-box__desc">
								<view><text>{{item.startTime}}</text></view>
								<view><text>{{item.place}}</text></view>
								<view><text>{{item.description}}</text></view>
								<view class="weui-media-box__add">
									<mp-icon type="outline" icon="add" color="block" size="{{9}}"></mp-icon>加入
								</view>
							</view>
						</view>
					</mp-cell>
				</mp-cells>
			</view>
		</scroll-view>
		<!-- 图书 -->
		<scroll-view class="tab-content" slot="tab-content-4" scroll-y="true" style="height: calc(100vh - 51px)"
			bindscrolltoupper="upper" bindscrolltolower="lower">
			<view class="tab-content__bd">
				<mp-cells>
					<mp-cell wx:for="{{bookList}}" wx:key="id" hover data-field="{{item.id}}" bindtap="handleView">
						<image wx:if="{{item.imgUrl}}" slot="icon" src="{{item.imgUrl}}" alt="封面"
							style="margin-right: 16px;vertical-align: middle;width:120px; height: 120px;" />
						<view wx:else slot="icon" style="margin-right: 16px;width:120px; height: 120px; background: #ccc" />
						<view class="weui-media-box__bd">
							<h4 class="weui-media-box__title">{{item.name}}</h4>
							<view class="weui-media-box__desc">
								<view><text>{{item.startTime}}</text></view>
								<view><text>{{item.place}}</text></view>
								<view><text>{{item.description}}</text></view>
								<view class="weui-media-box__add">
									<mp-icon type="outline" icon="add" color="block" size="{{9}}"></mp-icon>加入
								</view>
							</view>
						</view>
					</mp-cell>
				</mp-cells>
			</view>
		</scroll-view>
		<!-- 知识 -->
		<scroll-view class="tab-content" slot="tab-content-5" scroll-y="true" style="height: calc(100vh - 51px)"
			bindscrolltoupper="upper" bindscrolltolower="lower">
			<view class="tab-content__bd">
				<mp-cells>
					<mp-cell wx:for="{{knowList}}" wx:key="id" hover data-field="{{item.id}}" bindtap="handleView">
						<image wx:if="{{item.imgUrl}}" slot="icon" src="{{item.imgUrl}}" alt="封面"
							style="margin-right: 16px;vertical-align: middle;width:120px; height: 120px;" />
						<view wx:else slot="icon" style="margin-right: 16px;width:120px; height: 120px; background: #ccc" />
						<view class="weui-media-box__bd">
							<h4 class="weui-media-box__title">{{item.name}}</h4>
							<view class="weui-media-box__desc">
								<view><text>{{item.startTime}}</text></view>
								<view><text>{{item.place}}</text></view>
								<view><text>{{item.description}}</text></view>
								<view class="weui-media-box__add">
									<mp-icon type="outline" icon="add" color="block" size="{{9}}"></mp-icon>加入
								</view>
							</view>
						</view>
					</mp-cell>
				</mp-cells>
			</view>
		</scroll-view>

	</mp-tabs>
</view>