<!--index.wxml-->
<view class="container" data-weui-theme="{{theme}}">
	<hill-gallery show="{{show}}" img-urls="{{imgUrls}}" current="{{current}}" bindgoto="handleEnter" bindchange="handleChange"></hill-gallery>
</view>